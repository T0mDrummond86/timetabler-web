---
id: entities-classes
title: Classes — adding and editing
category: Classes, groups and qualifications
keywords: class, unit, new class, add class, edit class, duration, hours, units column, class name, double session, two bookings, first session hours, split a class, second sitting
---

A **class** is the thing you place on the timetable. Open the **Classes** tab
to manage them.

Select a class from the list to edit:

- **Class name** — what shows on the placecard.
- **Units** — the unit codes this class delivers. One class often covers
  several units clustered together.
- **Duration** — how long the class runs. Every placecard for the class uses
  this length.
- **Double session** — splits the class into two bookings. **First session
  (hours)** sets how the time divides; the second sitting gets the remainder.
  The split can be as uneven as you like — a 3-hour class can run 2.5 + 0.5 as
  readily as 1.5 + 1.5 — the only requirement being that the second sitting
  gets something. Tick *same day* to keep both halves on one day.
- **Qualifications** — which qualifications the class belongs to. A class can
  belong to more than one.
- **Allowed rooms** and **allowed lecturers** — see
  [Restricting rooms](#entities-allowed-rooms) and
  [Allowed lecturers](#staff-competency).

**New class** creates one. A class name is required.
